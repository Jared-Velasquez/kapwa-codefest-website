export type Org = {
    id: string,
    name: string,
    description: string,
    img: string,
    website: string,
}

export type TimeEvent = {
    id: string,
    time: string,
    name: string,
    description: string,
}